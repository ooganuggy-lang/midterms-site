# Get a free key in seconds at https://api.data.gov/signup/
# The DEMO_KEY works but is rate-limited to ~30 requests/hour - fine for
# local testing, not enough for a real deployed site.
FEC_API_KEY=DEMO_KEY

# The election cycle year to query (even years only)
ELECTION_CYCLE=2026

PORT=3001
